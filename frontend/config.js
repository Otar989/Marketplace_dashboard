// Global configuration for the Marketplace Dashboard frontend.
// Set `API_BASE_URL` to the publicly accessible backend URL when deploying
// the frontend (e.g. GitHub Pages). Leave it empty to use the defaults below.
window.APP_CONFIG = {
  // Example: 'https://your-backend-domain.example.com'
  API_BASE_URL: '',
  // Port for local development backend when serving the frontend locally.
  LOCAL_BACKEND_PORT: 3001,
};
